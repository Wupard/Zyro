rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/progress_photos/{photo} {
      // Only the owner can read/write their photos
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
